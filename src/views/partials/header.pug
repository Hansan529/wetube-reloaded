header.header
  a(href="/").header__logo
    i.fab.fa-youtube
  nav  
    ul 
      li 
        a(href="/search")
          i.fas.fa-search
      if loggedIn
        li
          a(href="/videos/upload").header__uploadBtn 동영상 업로드
            i
        li 
          a(href="/users/edit").header__editBtn #{loggedInUser.name}
            i
        li 
          a(href="/users/logout").header__logoutBtn Logout
            i
        li 
          a(href=`/users/${loggedInUser._id}`) 
            if !loggedInUser.avatarUrl
              span 😀
            else
              img(src=loggedInUser.avatarUrl, crossorigin="anonymous", alt="").header__avatar 
      else 
        li
          a(href="/login").loginBtn Login
        li
          a(href="/join").header__btn Join