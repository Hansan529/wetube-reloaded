extends ../base
include ../mixins/video

block content
    div.profile__data
        if !loggedInUser.avatarUrl
            span 😀
        else
            img(src=user.avatarUrl crossorigin="anonymous").avatar
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message 어떠한 동영상도 없습니다.